name: Build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch: # Manual trigger

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-java@v5
        with:
          java-version: 17
          distribution: 'temurin'
      - uses: gradle/actions/setup-gradle@v5
      - name: Compile
        run: ./gradlew compileJava -Perrorprone.enabled=false --no-daemon

  lint:
    runs-on: ubuntu-latest
    needs: [ compile ]
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-java@v5
        with:
          java-version: 17
          distribution: 'temurin'
      - uses: gradle/actions/setup-gradle@v5
      - name: Lint
        run: ./gradlew compileJava compileTestJava spotlessCheck --no-daemon

  test:
    runs-on: ubuntu-latest
    needs: [ compile ]
    strategy:
      matrix:
        java-version: [ '17', '21', '25' ]
    steps:
      - uses: actions/checkout@v5

      - name: Setup JDK ${{ matrix.java-version }}
        uses: actions/setup-java@v5
        with:
          java-version: ${{ matrix.java-version }}
          distribution: 'temurin'

      - uses: gradle/actions/setup-gradle@v5

      - name: Test
        run: ./gradlew test -Perrorprone.enabled=false --no-daemon
